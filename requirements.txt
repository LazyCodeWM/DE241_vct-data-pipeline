# Core
python-dotenv==1.0.0

# Ingestion / Bronze (already used)
vlrdevapi
pydantic==2.7.0
boto3==1.34.0

# Transformation / Silver + Gold
pyspark==3.5.1
polars==0.20.0
pyiceberg[s3filesystem,nessie]==0.7.0
pyarrow==15.0.0

# Orchestration
apache-airflow==2.9.1

# Dashboard
streamlit==1.35.0

# Dev tools
pytest==8.2.0
ruff==0.4.4
